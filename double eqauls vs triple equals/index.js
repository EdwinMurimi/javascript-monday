// you should understand this concept more.

const num1 = 1;
const num2 = '1';

// what this does it tries to convert the same variables into the same data type and equates them
console.log(num1 == num2);

// this compares the variables as well as their data types
console.log(num1 === num2);